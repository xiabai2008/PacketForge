---
name: Bug report
about: Report a problem with PacketForge
title: "[bug] "
labels: bug
assignees: ""
---

**Environment**

- OS:
- Python version:
- tshark version (`tshark --version`):
- nmap version (`nmap --version`):

**What happened?**

A clear description of the problem, including the exact tool call and its
returned envelope (`tool`/`status`/`error`/`audit_id`).

**Expected behavior**

What you expected to happen instead.

**Minimal reproduction**

Steps or a short snippet. Do **not** paste real credentials, keys, or
unauthorized target information.

**Logs / audit chain**

If available, attach the relevant entries from `AuditLog.report()` (they
contain no parameters, only hashes).
